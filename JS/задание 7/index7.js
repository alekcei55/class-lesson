const soors  = [
{ "name": "стол",'price': 5000 ,quantity: 5 },
{ "name": "кресло",'price': 1200 ,quantity: 7 },
{ "name": "монитор",'price': 7600 ,quantity: 8 },
{ "name": "кавиатура",'price': 500 ,quantity:  15 },
]; 

const max_price=5400

for ( thing of soors  ){ 
   thing['total'] = thing.price * thing.quantity; 
    if (thing['price'] <= max_price) { //подходящая цена 
     
    } else { 
( thing['price'] >= max_price)// слишком большая цена !!!!! нет денег!!!!!!
      }  
     } 
console.log(soors) 

for(const soor of soors);


console.log(name, price, quantity,poor)
if(soor.price <= max.price)

 console.log()









